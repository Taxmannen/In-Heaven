using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeProjectileColour : MonoBehaviour
{

    Renderer projectileColour;

    void Start()
    {
        projectileColour = GetComponent<Renderer>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
